const mitraFirstQuestion = require('./mitra-first-question');
const mitraSecondQuestion = require('./mitra-second-question');
const assert = require('assert');

it('', () => {
    assert.equal(mitraFirstQuestion.getHighestProductBetweenThreeNumbersInArray([1, 10, 2, 6, 5, 3]), 300);
})

it('', () => {
    mitraSecondQuestion.init();
})
